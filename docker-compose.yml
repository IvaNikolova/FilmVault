services:
  app:
    container_name: filmvault
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_TMDB_API_KEY=${VITE_TMDB_API_KEY}
    stdin_open: true
    tty: true
